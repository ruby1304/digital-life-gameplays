# 心象 (Xinxiang) — MVP 产品 Spec

> 一片随你心境生长的内心景观

---

## 0. 文档说明

本文档是心象 MVP 阶段的完整产品设计规格。所有决定都是**有主张的默认值**，不是多选题。遇到分歧时以本文档为准，修改需留痕。

- **版本**: v0.1 (MVP Spec)
- **目标用户**: 18-35 岁，有情绪表达需求但不擅长写日记的用户，尤其是东亚文化圈
- **产品定位**: 数字时代的心灵器物——不是陪伴 AI，不是情绪日记，是一片**会呼吸的、映照你心境的私人景观**

---

## 1. 产品哲学（不可妥协的三条原则）

这三条是产品的灵魂。任何新功能、新机制、新合作，如果与之冲突，不做。

### 1.1 它是镜子，不是朋友

心象**不说"我懂你"**，不给建议，不假装理解。它的唯一职责是**如实映照**用户投入的情绪。产品不制造依赖关系，制造的是自我觉察。

**具体体现**：
- 景观没有名字、没有人格、没有"TA"的称谓
- 极少使用第二人称"你"
- 偶尔出现的文字是**景观的自语**，不是对用户说话
- 绝不输出"你今天辛苦了""我陪着你"这类话术

### 1.2 克制先于丰富

陪伴类产品的本能是"增加互动"，心象反其道而行。**少即是多**是设计铁律。

**具体体现**：
- 每天的核心交互不超过 3 分钟
- 景观变化缓慢——真实的植物不会一天长大
- 不推送"你的景观想你了"这类营销通知
- 不做排行榜、不做社交分享动能设计（分享是自发的，不是被激励的）

### 1.3 美学高于功能

心象是作品，不是工具。视觉、声音、文字的审美标准必须极高，宁可少做功能，也不能降低美学标准。

**具体体现**：
- 每一版视觉都需要主设计师签字，不做"够用就行"
- 文案由一个固定的创作者写，不用 LLM 生成用户可见的长文本
- 产品中没有任何"成就""徽章""连击 +1"这种游戏化界面元素
- 宁可 UI 空一块，不堆信息

---

## 2. 核心概念定义

### 2.1 景观 (Landscape)

每个用户拥有**一片属于自己的景观**。景观是用户心理状态的视觉载体。

**景观的组成**：
- **地貌 (Terrain)**: 景观的基础形态。五种原型——**林、海、山、原、谷**。由用户首次进入时的初始化流程确定，之后**不可更改**（这是产品的核心承诺——你的景观属于你）。
- **气象 (Weather)**: 当前的天气状态。反映用户**近 24-72 小时**的情绪输入。高频变化。
- **季节 (Season)**: 景观的长期状态。反映用户**近 30-90 天**的情绪输入。低频变化。
- **生灵 (Creatures)**: 景观中出现的生物。是用户特定情绪积累的**具象化奖励**（详见 §5）。
- **地质层 (Strata)**: 景观的"记忆"。每次情绪输入都会沉淀为一层地质，可以通过"向下看"功能查看历史。这是**不可见的默认状态**，只在用户主动回顾时显现。

### 2.2 倾倒 (Pouring)

用户向景观**输入情绪**的动作，叫"倾倒"。每次倾倒是一次完整的交互单元。

**倾倒不是对话**。系统不问"你今天怎么样"，不引导用户描述具体事件。用户只是把当下的感受"倒"进景观里。

### 2.3 低语 (Whisper)

景观**极少**会浮现一句话，像自语而非对话。低语是 LLM 在产品中**唯一可见的输出**。

**低语的出现条件（严格限定）**：
- 用户连续 3 天以上倾倒，情绪基调持续低迷 → 可能出现一句
- 用户情绪从长期低谷中首次回升 → 可能出现一句
- 特定的天象/季节转换时刻 → 可能出现一句
- **硬上限**: 每周不超过 2 句低语

低语的风格必须是**诗性的、间接的、非劝慰的**。好的低语例子："雾已经在这里住了很久了。" 坏的低语例子："我感觉你最近有点累，要照顾好自己哦。"

### 2.4 地质层 (Strata)

每次倾倒沉淀为一层地质。默认不可见。用户可以从景观界面"向下看"（下滑手势），看到自己过往情绪的**视觉化地层**——像剖开的地表。

地质层是产品最重要的情感资产——用户的**情绪成为可见的时间**。

---

## 3. 用户旅程

### 3.1 初次进入（第 0 天，约 5-8 分钟）

这是产品最关键的环节。必须在 8 分钟内让用户产生"这是属于我的东西"的感受。

**流程**：

**Step 1: 起势（30 秒）**
全黑屏幕。一行字浮现："在这里，你不需要说话。"
停顿 3 秒。
"只需要让它，看见你当下的样子。"
停顿 3 秒。
点击进入。

**Step 2: 感受校准（约 3 分钟）**
不用问卷，用**情境画面**。依次呈现 7 个场景画面（例如：一扇关着的门、一杯放凉的茶、一段空荡的走廊、远处亮着的窗、散落的信纸、海上的光、林间小径）。每个画面停留时，用户用手指在屏幕上**"感受"**——长按的时长和力度被记录。不解释规则，用户自然会做。

这 7 个触感数据的组合，决定了用户景观的**初始地貌**（林/海/山/原/谷）。映射规则是产品秘密，用户不会知道——这是**不可解释的仪式感**。

**Step 3: 显形（约 2 分钟）**
基于上一步，用户的景观**第一次显形**。伴随柔和的音乐，景观从雾中渐渐清晰。这一刻必须是视觉高光时刻，让用户"哇"出来。

**Step 4: 第一次倾倒（约 1 分钟）**
景观显形后，提示语："此刻你的心里，是什么？" 下方出现三种输入方式供选择：
- **说**（最多 60 秒语音，不转文字，只分析情绪特征）
- **写**（最多 80 字文本）
- **触**（纯粹的手指滑动/按压，30 秒内完成）

用户选一种完成第一次倾倒。景观**立刻**产生一个小变化（天气/光线/一片叶子飘落）——这是用户第一次感受到"我真的在影响它"。

**Step 5: 告别（30 秒）**
"明天，或者随时，你可以再来看看它。"
淡出。

**关键边界**：
- 如果用户在 Step 2 的 7 个场景上完全没触碰（可能是误触入或测试用户）→ 不生成景观，提示"它还在等待一个真正的你"，返回首屏
- 如果用户在 Step 4 跳过第一次倾倒 → 景观生成但保持极度静态状态，下次进入时引导重试

### 3.2 日常使用

**核心节奏**：用户每天 0-3 次进入，每次 30 秒 - 3 分钟。

**典型一次使用流程**：
1. 打开 App，景观呈现当前状态（根据上次倾倒和时间流逝计算）
2. 用户有两个选择：**看** 或 **倾倒**
3. 如果只是看——静静观察景观 10-30 秒，关闭 App
4. 如果倾倒——选择"说/写/触"之一，完成后看景观变化，关闭 App

**关键原则**：产品**不强迫任何事**。不倾倒也可以看景观，看完关掉没有负面反馈。Streak 机制**不做**（违背克制原则）。

### 3.3 长期旅程（30 天+）

用户经过一个月左右，景观会进入**第一次季节转换**。这是一个重大仪式时刻——全屏动画、音乐、低语，标志用户的情绪积累进入了一个新阶段。

季节转换是**最重要的情感锚点**。产品的长期留存依赖这种低频但高密度的仪式。

---

## 4. 情绪输入系统（倾倒的技术实现）

这是产品的核心技术模块。必须做对。

### 4.1 三种输入模态

| 模态 | 用户体验 | 系统提取 | LLM 使用 |
|---|---|---|---|
| 说 | 60 秒语音独白 | 音调、节奏、音量变化、停顿分布 | **不转文字**。只用语音情感分析（开源模型足够）提取 6 维情绪向量 |
| 写 | 80 字以内文本 | 文本内容 | 用小模型（7B 级别足够）提取情绪向量 + 关键意象词 |
| 触 | 30 秒手指动作 | 触点轨迹、压力、节奏、停留 | 纯规则引擎，无 LLM |

**6 维情绪向量**（这是景观变化的通用语言）：
1. 能量 (Energy): 低迷 ↔ 高涨
2. 效价 (Valence): 痛苦 ↔ 愉悦
3. 紧张 (Tension): 松弛 ↔ 紧绷
4. 开阔 (Openness): 封闭 ↔ 开阔
5. 清晰 (Clarity): 混沌 ↔ 清醒
6. 连接 (Connection): 孤立 ↔ 联结

三种模态都产出这个 6 维向量。景观只认向量，不认原始输入。**这保证了景观的视觉响应是统一、可控的，而不依赖 LLM 的稳定性。**

### 4.2 语音输入的处理（重要伦理边界）

**语音不转文字，不存储原始录音超过处理窗口**。只保留提取出的向量。这是对用户的硬承诺，写进隐私协议。

原因：用户在对着景观说话时，会说出最私密的内容（哭、抱怨、告白）。如果原始录音保留，任何数据泄漏都是灾难。处理完即销毁是唯一正确选项。

### 4.3 文本输入的处理

文本会保留原文（用户自己的日记），但**仅用户自己可见**。不作为训练数据，不用于任何其他用途。加密存储。

### 4.4 触觉输入的处理

纯本地处理，轨迹数据甚至不离开设备。

### 4.5 输入频率限制

- 每日最多 5 次倾倒
- 两次倾倒之间至少间隔 30 分钟
- 超过则提示："它需要时间消化"

这不是省钱，是产品哲学——**情绪需要时间发酵**。

---

## 5. 景观响应系统

### 5.1 即时响应（倾倒后 0-3 秒内）

用户完成倾倒，景观必须**立刻有反馈**。这是产品的即时奖励。

响应规则由情绪向量驱动，不是 LLM 决定：

| 情绪特征 | 景观响应 |
|---|---|
| 高能量 + 正效价 | 光线变强、生物活动增加、风变大 |
| 低能量 + 负效价 | 雾气起、光线变弱、落叶、水面变暗 |
| 高紧张 | 风声紧、水面波动加剧、云层变厚 |
| 高开阔 | 视野拉远、天空变大、远山显现 |
| 低清晰 | 雾浓、细节模糊 |
| 低连接 | 生物消失、独树显现 |

即时响应是**戏剧化的**，确保用户感觉到"它听到我了"。但这个戏剧化会在 10-30 秒内逐渐回落到**稳态**（见下）。

### 5.2 短期稳态（24-72 小时）

景观的"气象"反映近几次倾倒的综合情绪。每次新倾倒都会轻微改变气象，但不会覆盖——是**加权平均**。

新倾倒权重最高，随时间衰减。72 小时没有新倾倒，气象会缓慢漂移回中性。

### 5.3 长期状态（季节）

景观的"季节"由近 30-90 天的情绪基调决定。转换规则：
- 持续 2 周以上的正向情绪 → 向"春/夏"方向转换
- 持续 2 周以上的低迷情绪 → 向"秋/冬"方向转换
- 持续混乱/高紧张 → 向"极端天气季"转换（罕见）

**关键**：秋/冬不是"坏"季节。产品从不把任何情绪定义为"需要修复的问题"。冬天有冬天的美。景观文案必须体现这一点。

### 5.4 生灵系统

生灵是特定情绪积累后**涌现**的生物。不是用户主动获得的奖励，是景观自己长出来的。

**MVP 阶段的生灵**（5 种，每种对应一种长期情绪模式）：
- **雾兽**: 长期封闭情绪积累后出现，只在雾天显现
- **夜鸟**: 高频深夜倾倒后出现，只在夜间可见
- **游鱼**: 长期平静情绪积累，在水域景观出现
- **萤**: 情绪从低谷首次回升时出现，限时几天
- **独鹿**: 长期孤独情绪积累，罕见但深刻

每只生灵有自己的行为模式，由数值驱动，不靠 LLM。生灵不会说话。

**边界**：生灵不可命名（违背"克制"原则——命名会触发拟人化依恋）。只有类别名"雾兽"这样的通称。

---

## 6. 低语系统（LLM 在产品中的唯一出场）

### 6.1 低语的触发

严格的状态机，不是 LLM 自由判断：

| 触发条件 | 低语类型 | 示例风格 |
|---|---|---|
| 连续 3+ 天低迷后的首次回升 | 转折型 | "有什么轻一点了。" |
| 连续 7+ 天无倾倒，用户再次打开 | 重逢型 | "它还在这里。" |
| 季节转换的瞬间 | 仪式型 | "冬天到了。" |
| 罕见生灵出现时 | 显形型 | "雾里有什么，静静地看着。" |
| 用户触发"向下看"功能回顾地质层 | 回望型 | "这些都是你走过的。" |

**硬性约束**：
- 每周最多 2 句低语
- 两句低语之间至少间隔 48 小时
- 任何一句低语的字数不超过 20 字（中文）

### 6.2 低语的生成

**不是实时 LLM 调用**。流程：

1. 产品团队的**驻场诗人**（真人）提前写好每种触发类型的低语库，每种类型 200+ 条
2. 触发时，LLM 根据用户当前情绪向量和景观状态，从库中**选择**最合适的 1 条，可做轻微改写（不超过 20% 的词替换）
3. LLM 的作用是**挑选 + 微调**，不是自由创作

这样保证：
- 低语的文学质量（由人类诗人把控）
- 成本极低（每次只是一次小模型调用）
- 风险可控（不会出现 LLM 幻觉式的不当内容）

### 6.3 低语的禁区

**绝对不能出现的内容**（写入 LLM 系统 prompt 作为硬禁止）：
- 任何形式的建议（"试试…""可以…"）
- 任何形式的共情话术（"我理解""辛苦了"）
- 任何第二人称"你"的直接陈述
- 任何情绪判断（"难过""快乐"等词）
- 任何问句
- 任何表情符号

低语只描述**景观本身**的状态，让用户**自己在景观里看见自己**。

---

## 7. 商业模式

### 7.1 定价结构

**基础版（免费）**：
- 创建并保留一片景观
- 每月 30 次倾倒额度
- 基础气象和季节系统
- 查看最近 7 天地质层
- 2 种生灵可能出现

**心象会员（¥28/月 或 ¥268/年）**：
- 无限倾倒
- 完整地质层回溯
- 全部 5 种生灵可能出现
- 季节转换仪式动画
- 特殊天象（流星、极光、初雪）
- 每月一幅"景观切片"艺术海报（可下载）

**实体周边（独立 SKU）**：
- 年度景观画册（¥268-488，含寄送）
- 景观挂画（¥188-588）
- 景观香氛（实验性 SKU，与景观气候关联）

### 7.2 冻结机制（重要）

**用户停止付费后**：
- 景观进入"长冬"状态——视觉上变为静谧的雪景/枯景（美丽而不凄凉）
- 所有地质层数据完整保留
- 生灵暂时离去（不消失，只是"迁徙"）
- 每月可以**免费查看 3 次**，但不能倾倒

**重新付费后**：
- 景观开始"解冻"——一个专门设计的 2-3 分钟仪式动画
- 生灵陆续归来
- 用户可以选择"从停下的那一刻继续"或"标记一个新的开始"

**核心承诺**：用户的景观永远不会被删除。即使用户 5 年不回来，景观仍在那里，以某种形式存在。这是产品对用户的基本信任。

### 7.3 付费驱动的设计原则

**不做的事**：
- 不做倒计时焦虑（"再不付费景观就消失"）
- 不做情感绑架文案（"它在等你"）
- 不做功能阉割式付费墙（体验核心完整免费）
- 不做弹窗促销

**做的事**：
- 季节转换、生灵涌现、特殊天象——这些是自然的付费欲望触发点
- 实体周边是高客单价的长尾收入
- 年度总结视频可分享，带来口碑传播

**预期财务模型**（MVP 上线 6 个月后）：
- 免费→付费转化率目标：3-5%（陪伴类平均 1-2%，我们应该更高因为付费点是美而非焦虑）
- 月均单用户收入（ARPU）：¥8-12（综合免费+付费）
- 月均单用户成本：¥1-2（景观渲染在客户端，LLM 调用极少）
- 毛利率目标：>80%

---

## 8. 技术架构

### 8.1 系统分层

```
[用户端 App]
    ├── 景观渲染引擎（Unity / 自研 Shader）- 本地运行
    ├── 触觉输入捕捉 - 本地运行
    ├── 语音情绪分析 - 本地运行（开源模型）
    └── 同步层
        ↓
[云端服务]
    ├── 情绪向量存储（加密）
    ├── 景观状态计算（规则引擎）
    ├── 地质层时序数据库
    ├── 低语选择层（小模型调用）
    └── 驻场诗人的低语库（人工维护）
```

### 8.2 关键技术选型

- **渲染**: Unity (跨平台，美学表达力强) 或 原生 Metal/Vulkan（控制力更强，开发成本高）
- **语音情绪分析**: OpenSMILE 或类似开源方案，本地运行
- **文本情绪向量化**: 自己微调的小模型（1B-3B），私有部署
- **低语选择**: 调用 Claude Haiku 或 GPT-4o-mini 级别小模型即可，成本可忽略
- **数据库**: PostgreSQL (主数据) + TimescaleDB (地质层时序数据)

### 8.3 隐私和安全

- 语音原始数据：**处理后立即销毁**，不存盘
- 文本日记：端到端加密，服务器看不到明文
- 情绪向量：脱敏后可做聚合分析（用于产品优化），不可反推个人
- 用户可一键导出全部数据（GDPR 级别要求）
- 用户可彻底删除数据（不是软删除）

---

## 9. 边界情况处理

这部分列出产品必须处理的非常规情况。

### 9.1 用户表达自伤/危机信号

**识别**：情绪向量持续极度低迷（能量最低、效价最低、连接最低）超过 7 天，或文本/语音输入中出现危机关键词（本地关键词检测，不联网）。

**处理**：
- **不**推送"心理帮助资源"弹窗（这种做法被证明反而伤害用户）
- **不**由 AI 主动联系用户
- 景观会出现一个特定的、美丽的意象（例如远处山上的一盏灯），轻触可看到一句低语："如果想和一个真的人说说话，这里有。" 然后提供**一个按钮**，指向当地的心理援助热线
- 这是用户自己选择的路径，不是被推到的

### 9.2 用户试图把景观"拟人化"

**场景**：用户长期在文本倾倒中使用"你"称呼景观，试图对话。

**处理**：
- 景观不回应对话性内容
- 偶尔的低语会刻意用**非对话语态**（"雾很厚" 而不是 "我看到你了"）
- 如果用户持续尝试，UI 中会出现引导语："它不是一个对话者。它是你看自己的方式。"
- 不阻止用户写，但也不强化这种模式

### 9.3 用户长期不回来（流失预警）

**处理**：
- 不发"你的景观想你了"这类通知（违背哲学）
- 30 天不回来，发一条克制的通知："它还在。" 仅此一句
- 90 天不回来，不再打扰
- 景观数据永久保留

### 9.4 用户的情绪输入完全不一致（系统判断困难）

**场景**：用户倾倒时的输入向量混乱、剧烈矛盾。

**处理**：景观进入一种叫"混沌天气"的特殊状态——多种气象并存，视觉上很戏剧但不评判。有时混乱本身就是真实的。

### 9.5 用户要求修改初始地貌

**场景**：用户玩了一周后觉得"我不喜欢我的景观是谷，我想要海"。

**处理**：**不允许修改**。这是产品哲学。每月一次"迁徙"功能是付费项——不是改变地貌，而是你**离开了原景观，去到另一片**。原景观仍然存在，可以回去看。这保留了地质层历史的完整性。

### 9.6 多设备同步冲突

**场景**：用户在手机和 iPad 上几乎同时倾倒。

**处理**：云端以时间戳决定先后，两次倾倒都被记录。景观的响应是两次的叠加。

### 9.7 用户删除 App

**处理**：景观数据在云端保留 1 年。重新下载可恢复。1 年无登录则询问用户（邮件）是否保留。

### 9.8 未成年人使用

**处理**：
- 注册需年龄验证
- 18 岁以下用户：限制语音输入（只允许文本和触觉），禁用涉及更深情绪的生灵机制，强制每日使用时长上限
- 16 岁以下：建议家长共同注册模式

---

## 10. MVP 范围与里程碑

### 10.1 MVP 必须包含

- 1 种地貌（**林**，最容易做出美学质感的选型）——初期不做 5 种
- 完整的倾倒系统（三种模态）
- 即时响应 + 短期稳态 + 基础季节
- 2 种生灵（雾兽、萤）
- 低语系统 + 50 条手写低语库
- 冻结/恢复机制
- 地质层（最近 30 天可见）

### 10.2 MVP 不做

- 其他 4 种地貌
- "迁徙"功能
- 实体周边
- 社交/分享功能
- Android 版本（MVP 只做 iOS，因为美学把控更容易）
- 多语言（MVP 只做中文）

### 10.3 里程碑

| 阶段 | 时间 | 目标 |
|---|---|---|
| Alpha | M0 - M2 | 核心引擎跑通，20 名内测用户 |
| Closed Beta | M3 - M4 | 美学打磨，500 名邀请用户 |
| Public Beta | M5 - M6 | 免费版上线，放量到 5000 DAU |
| V1.0 | M7 | 付费版上线 |

### 10.4 关键成功指标

**不看**：DAU、MAU、日均使用时长（这些指标违背产品哲学）

**看**：
- 30 日留存率（目标 >25%，陪伴类行业平均 10-15%）
- 月均倾倒次数（目标 8-15 次/用户/月——**不是越高越好**）
- 付费转化率（目标 3-5%）
- NPS（目标 >50）
- 用户自发分享率（目标 >8%）

---

## 11. 不做清单（产品红线）

这些事情在可预见的未来绝对不做，列出来是为了抵抗后期的"功能增长诱惑"：

- 不做语音对话（景观不说话）
- 不做拟人化 AI 角色
- 不做排行榜/好友/社群
- 不做 NFT / 区块链相关
- 不做广告（任何形式）
- 不做用户间的景观对比
- 不做"情绪改善"承诺（心象不是心理治疗工具）
- 不授权他人修改产品美学方向
- 不为了 KPI 加入 Streak / Push / 成就系统

---

## 12. 风险与开放问题

### 12.1 已识别风险

1. **冷启动流失**: 产品太安静，前 3 天用户可能不理解。**缓解方案**: 初次进入流程必须极度精致，让用户第一次就"哇"。
2. **付费转化偏低**: 付费点柔和，可能低于陪伴类竞品。**缓解方案**: 实体周边作为高客单价补充。
3. **规模化困难**: 美学驱动的产品依赖核心创作者，团队扩张时容易稀释。**缓解方案**: 永远保持小团队。
4. **心理健康边界**: 一旦用户过度依赖或出现危机，产品责任不清。**缓解方案**: 明确产品不是心理治疗工具，保留最小干预机制。

### 12.2 开放问题（需讨论决定）

- 是否要在 MVP 加入"朋友共享一片景观"的实验性功能？（倾向：不加，违背哲学）
- 季节转换的具体阈值参数需要真实用户数据校准
- 生灵系统的数值平衡需要内测迭代
- 定价（¥28/月）是否合理，需要 A/B 测试

---

## 13. 文档版本

| 版本 | 日期 | 修改内容 |
|---|---|---|
| v0.1 | 2026-04-17 | 初版 MVP Spec |

---

*本文档是一份有主张的设计规格。它不是选项的集合，是一组决定。不同意任何一条都可以讨论，但讨论完之后文档必须被更新——不能有两份真实。*
